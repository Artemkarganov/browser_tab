<h1>Your URLs:</h1>
<%= link_to 'Add New URL', new_url_path %>

<%= form_tag urls_path, :method => 'get', :id => "urls_search" do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>
<ul>
  <% @urls.each do |p| %>
    <li>
      <%= p.url %>
      <%= link_to '×', url_path(p), method: :delete, remote: true %>
    </li>
  <% end %>
</ul>
<%= will_paginate @urls %>
